/**
 * ThemeToggle 组件 Props
 */
export interface ThemeToggleProps {
  /** 图标尺寸，默认 20 */
  size?: string | number;
  /** 日间模式图标，默认 'Sun' */
  dayIcon?: string;
  /** 夜间模式图标，默认 'Moon' */
  nightIcon?: string;
}
