/** QLayout 组件 Props（对齐 Ant Design Layout） */
export interface LayoutProps {
  /**
   * @property hasSider
   * @defaultValue 自动检测
   * @description 是否强制按「含 Sider」的横向方式渲染；缺省时自动检测内部是否直接含 QSider
   */
  hasSider?: boolean;
}
