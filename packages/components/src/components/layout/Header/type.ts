/** QHeader 组件 Props */
export interface HeaderProps {
  /**
   * @property height
   * @defaultValue 无
   * @description 顶栏高度（px）；缺省使用 CSS 变量 --q-layout-header-height（默认 56）
   */
  height?: number;
}
